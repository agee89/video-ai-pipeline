# Terminal 1: Monitor logs
docker logs video_worker -f

# Terminal 2: Submit job
curl -X POST http://localhost:8000/process_video -H "Content-Type: application/json" -d '{"youtube_url":"https://www.youtube.com/watch?v=jNQXAC9IVRw","topics":["any"],"portrait":true}'
```

Anda akan lihat log detail seperti:
```
Original dimensions: 1920x1080
Target dimensions: 608x1080 (9:16)
Crop position: x=656, y=0
Running FFmpeg command...
Final dimensions: 1080,1920


# Get clip URL from result
curl http://localhost:8000/job/JOB_ID | jq -r '.result.clips[0].url'

# Download
curl -o test_portrait.mp4 "http://localhost:9000/video-clips/xxx.mp4"

# Check dimensions (harus 1080x1920 atau ratio 9:16)
ffprobe -v error -select_streams v:0 -show_entries stream=width,height -of csv=p=0 test_portrait.mp4

# Check quality
ffprobe -v error -select_streams v:0 -show_entries stream=bit_rate -of default=noprint_wrappers=1:nokey=1 test_portrait.mp4